{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cosmos": {
    "__comment": "Vide = DAL In-Memory (démo). Renseigner Endpoint + Key (via user-secrets, jamais ici) pour activer Cosmos DB.",
    "Endpoint": "",
    "Key": ""
  },
  "Stripe": {
    "__comment": "Secrets réels via Key Vault / user-secrets — laisser vide ici. Si WebhookSecret est vide, la vérification de signature est désactivée (dev uniquement).",
    "SecretKey": "",
    "WebhookSecret": ""
  },
  "WhatsApp": {
    "__comment": "Secrets réels via Key Vault / user-secrets — laisser vide ici. Si AppSecret est vide, la vérification de signature est désactivée (dev uniquement).",
    "VerifyToken": "",
    "AppSecret": ""
  }
}
